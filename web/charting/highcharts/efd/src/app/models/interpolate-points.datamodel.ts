export class InterpolationPoints {
  xAxis: number;
  yAxis: number;
  zAxis: number;

  constructor(xAxis: number, yAxis: number, zAxis: number) {
    this.xAxis = xAxis;
    this.yAxis = yAxis;
    this.zAxis = zAxis;
  }
}
